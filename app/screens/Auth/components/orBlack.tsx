import { styles } from "@/app/theme/appTheme";
import { View, Text } from "react-native";

export const OrBlack = () => {
  return <Text style={[styles.textColorBlack, { marginTop: 30 }]}>Or</Text>;
};
