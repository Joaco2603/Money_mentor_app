import { styles } from "@/app/theme/appTheme";
import { View, Text } from "react-native";

export const OrWhite = () => {
  return <Text style={[styles.textColorWhite, { marginTop: 30 }]}>Or</Text>;
};
